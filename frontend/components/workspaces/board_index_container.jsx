import {connect} from 'react-redux';
import BoardIndex from './board_index';
// import { fetchWorkspaces } from '../../actions/workspace_actions'

const mSTP = (state) => ({
    // boards: state.entites.workspaces[ownProps.match.params.workspace_id]  
})

// const mDTP = (dispatch) => ({
    
// })

export default connect(null, null)(BoardIndex)